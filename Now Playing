<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>📻 Live Radio</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background-color: #121212;
      color: #fff;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    h1 {
      font-size: 2em;
      margin-bottom: 20px;
    }
    button {
      font-size: 60px;
      background: none;
      border: none;
      color: #1DB954;
      cursor: pointer;
    }
    input[type="range"] {
      width: 80%;
      margin-top: 20px;
    }
  </style>
</head>
<body>

  <h1 id="nowPlaying">Live Station</h1>
  <button id="playBtn">▶️</button>
  <input type="range" id="volume" min="0" max="1" step="0.05" value="1">

  <!-- Hier komt de live audio stream -->
  <audio id="radioPlayer" src="LIVE_STREAM_URL" preload="auto"></audio>

  <script>
    const playBtn = document.getElementById('playBtn');
    const volume = document.getElementById('volume');
    const radioPlayer = document.getElementById('radioPlayer');
    const nowPlaying = document.getElementById('nowPlaying');

    playBtn.addEventListener('click', () => {
      if (radioPlayer.paused) {
        radioPlayer.play();
        playBtn.textContent = "⏸️";
      } else {
        radioPlayer.pause();
        playBtn.textContent = "▶️";
      }
    });

    volume.addEventListener('input', () => {
      radioPlayer.volume = volume.value;
    });

    // Optioneel: "Now Playing" info uit jouw backend laden
    // Voorbeeld (fake data):
    nowPlaying.textContent = "Nu speelt: 🎶 David Guetta - Beautiful People";
  </script>

</body>
</html>
